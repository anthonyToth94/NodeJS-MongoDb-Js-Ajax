<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="style.css" />
  </head>
  <body>
    <div class="container">
      <header>
        <form id="addItem">
          <h1>SHOPPING LIST</h1>
          <input
            type="text"
            name="items"
            id="items"
            placeholder="Items..."
            autocomplete="off"
            required
          />
          <input type="submit" value="+" id="add" />
        </form>
      </header>

      <% if(list != "") { %> <%- include('main'); %> <% } %>
    </div>
    <script src="javascript.js"></script>
  </body>
</html>
